package game;
import javax.swing.*;

public class Window {

        public Window(){
        JFrame frame = new JFrame("No Libraries Needed!");
        frame.setSize(800,600);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setVisible(true);
    }
}